# Optional FCM transport for anonymous mobile apartment preset notifications.
# Base64 encode the Firebase service-account JSON before placing it in runtime env.
FIREBASE_SERVICE_ACCOUNT_B64=
MOBILE_SUBSCRIPTION_POLL_SECONDS=60
MOBILE_SUBSCRIPTION_MAX_NOTIFICATIONS_PER_SCAN=8
