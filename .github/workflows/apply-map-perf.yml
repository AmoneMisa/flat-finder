name: Apply map perf patch

on:
  push:
    branches:
      - perf/flutter-map-clustering

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v7
        with:
          ref: perf/flutter-map-clustering
          fetch-depth: 0

      - name: Apply focused MapView patch
        run: python .github/scripts/apply-map-perf.py

      - name: Commit generated patch
        run: |
          if git diff --quiet -- app/lib/widgets/map_view.dart; then
            echo "No MapView changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/lib/widgets/map_view.dart
          git commit -m "perf(app): spatially index map marker rendering"
          git push origin HEAD:perf/flutter-map-clustering
