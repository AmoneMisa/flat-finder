name: Apply web platform audit fixes

on:
  push:
    branches: [ develop ]
    paths:
      - '.github/scripts/apply-web-platform-audit.py'
      - '.github/workflows/apply-web-platform-audit.yml'

permissions:
  contents: write

concurrency:
  group: apply-web-platform-audit
  cancel-in-progress: false

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout develop
        uses: actions/checkout@v7
        with:
          ref: develop
          fetch-depth: 0

      - name: Apply asserted replacements
        run: python .github/scripts/apply-web-platform-audit.py

      - name: Commit patched app
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/lib
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git commit -m 'fix(app): make platform checks web-safe'
          git push origin HEAD:develop
